# Default: accessible on the Linux host, or behind a reverse proxy.
LUMEN_BIND_ADDRESS=127.0.0.1
LUMEN_PORT=8080
# Optional HTTP Basic authentication. Set both values together.
LUMEN_AUTH_USER=
LUMEN_AUTH_PASSWORD=
