# 部署与验证记录

## 2026-09-06：正式部署完成

- 正式网址：https://lumen-vercel-neon.vercel.app
- 项目：`haruto-nitos-projects/lumen-vercel`，Hobby 计划。
- 部署 ID：`dpl_Bkfim2cD2gDm9RdofDwLFZQWRR1g`。
- 函数：Node.js 24，东京 `hnd1`，单入口 `api/gateway`。
- 原工作区未修改。登录配置、环境变量文件、Git 目录、测试数据和 Windows 构建产物均未上传。

实际部署中修复了两个问题：多层 API 地址需要显式重写到函数入口；SDK 获取播放地址前需要初始化公开加密密钥，不能依赖开发机器已有的临时缓存。初始化支持并发复用、超时和失败重试，已通过完全空白临时目录下的真实播放地址测试及 3 项新增回归测试。

最终正式公开域名验收通过：

- 健康检查、真实搜索、歌曲详情、播放地址、扫码 Key、二维码 PNG 和未登录状态接口。
- 未知接口返回 JSON 404，跨站调用返回 403。
- 浏览器实际在线播放公开歌曲样本，确认音频播放时间前进。
- 中、英、日三种语言的搜索、隐藏 API 地址、本地 WAV 播放、字体与 CSP 检查。

未使用用户网易云账号完成真人扫码登录，也未验证会员或受限歌曲的账号权限。部分歌曲返回不可用地址，播放器仍遵循上游返回结果。免费额度尚未做长期观察，没有开启付费计划、购买域名或服务器。

验收脚本为 `tools/vercel-cloud-check.mjs`；正式站点截图和结果位于 `test-results/cloud-*.png`、`test-results/vercel-cloud.json`。

## 历史记录：2026-09-05 部署前本地验证

日期：2026-09-05。子工作区：`E:\Lumen\workspaces\vercel`。

## 已完成

- 独立 Git 仓库，分支 `vercel`；修改前源码快照提交 `a4cecac`。
- 核对父工作区全部 60 个基线源码文件，内容与复制时一致。
- Node.js 24.19.0：类型检查、Vercel 静态资源构建、原单文件构建通过。
- 12 项服务/API 测试通过，覆盖 Vercel 解析请求体、原始流、并发 Cookie 隔离、冷启动重试、路由白名单、请求上限、跨站拒绝和二维码 PNG。
- Vercel 官方 Node 构建器 12.0.1 生成 `nodejs24.x` 函数包，包含 1,418 个文件；模块解析限制在函数包内部，启动与二维码生成功能通过。函数包不包含 Electron、electron-builder 或 React。
- 中、英、日三种语言的浏览器检查通过：静态字体/封面、隐藏 API 地址、CSP、旧 HTTP 封面升级为 HTTPS、动态 WAV 解析、本地播放和媒体元数据。
- 本机通过新函数入口发起真实网易云搜索和未登录状态请求，均成功。
- `npm audit --omit=dev`：0 个已报告的生产依赖漏洞。此结果不表示开发工具依赖也没有漏洞。

## 未执行

- 未关联 Vercel 账号，未上传项目，未创建预览或生产部署。
- 未验证 Vercel 东京区域的真实网络出口、真人扫码登录、账号权限歌曲播放或免费额度的实际消耗。
- 新增 Ubuntu CI 流程已写入仓库，本次未在 GitHub 运行。

浏览器截图和函数包检查结果位于忽略提交的 `test-results/`。部署步骤见 [VERCEL.md](VERCEL.md)。
