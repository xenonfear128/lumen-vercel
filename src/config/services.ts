// All shipped clients use their own service origin. Desktop binds it to loopback;
// Linux serves the same routes on the server, including through an HTTPS proxy.
export const NETEASE_API_ORIGIN = "/api";
